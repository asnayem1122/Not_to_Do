# Not To Do — local development environment template.
#
# Copy this file to `.env` and paste your own Gemini API key for local
# debugging. `.env` is git-ignored, so your key never reaches version control.
#
# Alternatively (recommended): tap the sparkle icon in the app header and
# paste your key there — it is stored in encrypted device storage instead.
GEMINI_API_KEY=
